const path = require('path');
const static='./static'

module.exports = {
    DIR : {
        thumb: `${static}/thumb`,
        imgs: `${static}/imgs`,
        avatar: `${static}/avatar`,
    },
    JWT : {
      expire_day:1
    }
  };